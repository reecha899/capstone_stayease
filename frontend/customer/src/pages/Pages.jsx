import React from 'react';
import { Box, Typography } from '@mui/material';

const Pages = () => {
  return (
    <Box sx={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '50vh' }}>
      <Typography variant="h4">Pages Page</Typography>
    </Box>
  );
};

export default Pages; 